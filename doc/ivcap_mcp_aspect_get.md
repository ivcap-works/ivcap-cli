## MCP tool: aspect_get

`aspect_get` is a **built-in** tool exposed by `ivcap mcp`.

It fetches a single aspect record by its URN/ID.

### Parameters

- `id` (required): Aspect URN/ID

### Example

```json
{ "id": "urn:ivcap:aspect:..." }
```
